public struct vert {
    public Vector3 pos;
    public Vector2 tex;
    public Vector3 norm;

    public vert(Vector3 _pos, Vector2 _tex, Vector3 _norm) {
        pos = _pos;
        tex = _tex;
        norm = _norm;
    }
}
